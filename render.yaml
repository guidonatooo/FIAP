services:
  - type: web
    name: api-embrapa-offline
    env: python
    plan: free
    buildCommand: pip install -r offline/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    workingDir: offline
    autoDeploy: true
